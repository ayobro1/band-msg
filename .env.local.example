JWT_SECRET=your-secret-key-here  # Generate with: openssl rand -base64 32
DATABASE_PATH=./data/band-chat.db
AUTH_COOKIE_SECURE=auto

# WebSocket server port (server-side)
WS_PORT=3001
# WebSocket port exposed to browser (must match WS_PORT)
NEXT_PUBLIC_WS_PORT=3001

# File uploads
UPLOADS_PATH=data/uploads
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@lazzycal.com

# Giphy API key (get from https://developers.giphy.com/)
GIPHY_API_KEY=
